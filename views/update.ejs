<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/html">
<head>
    <title>RAYBARO</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <script src="https://code.jquery.com/jquery-2.2.4.min.js"></script>
</head>
<%var read = rows[0]; %>
<header>
    <div class="wrapper">
        <h1><a href="#"><span>Raybaro </span><span class="id">Database System</span> </a></h1>
    </div>
</header>

<nav>
    <ul class="menu">
        <li><a href="/board/page/1" id="menubar">메인화면</a></li>
        <li><a href="/board/write" id="menubar">자료입력</a></li>
        <li><a href="/board/search_home" id="menubar">검색하기</a></li>
    </ul>
</nav>

<body>

<form method="post" action="/board/update/<%= read.idx %>" onsubmit="onWriteSubmit()" enctype="multipart/form-data">
    <table border="2">
        <thead>
        <tr>
            <td colspan="5" id="main">Repair List</td>
        </tr>

        </thead>
        <tbody>
        <tr>
            <th>수리 분류</th>
            <td>
                <select name='sort' id=sort' style='width:50%;height:100%;'>
                    <% for (var i=0; i < sort_data.length; i++) { %>
                        <option value=<%= i + 1 %> <%= read.sort == i+1 ? 'selected' : ''%> > <%=sort_data[i].name%> </option>
                    <% } %>
                </select>
            </td>
            <th>수리 요청 회사</th>
            <td>
                </select>
                <select name='recompany' id=recompany' style='width:50%;height:100%;'>
                    <% for (var i=0; i < recompany_data.length; i++) { %>
                        <option value=<%= i + 1 %> <%= read.recompany == i+1 ? 'selected' : ''%> > <%=recompany_data[i].name%> </option>
                    <% } %>
                </select>
            </td>
        </tr>

        <tr>
            <th>최초 고객사 및 담당자</th>
            <td><input type="text" name="writer" value="<%=read.writer%>" style="width:50%;height:50%;" required/></td>

            <th>모델명</th>
            <td>
                <select name='title' id=title' style='width:50%;height:100%;'>
                    <% for (var i=0; i < title_data.length; i++) { %>
                        <option value=<%= i + 1 %> <%= read.title == i+1 ? 'selected' : ''%> > <%=title_data[i].name%> </option>
                    <% } %>
                </select>
            </td>
        </tr>

        <tr>
            <th>장비 제조사명</th>
            <td>
                <select name='gearcompany' id=gearcompany' style='width:50%;height:100%;'>
                    <% for (var i=0; i < gearcompany_data.length; i++) { %>
                        <option value=<%= i + 1 %> <%= read.gearcompany == i+1 ? 'selected' : ''%> > <%=gearcompany_data[i].name%> </option>
                    <% } %>
                </select>
            </td>
            <th>제품 코드 번호</th>
            <td><input type="text" name="codenum" value="<%=read.codenum%>" style="width:50%;height:50%;font-size:15px;" required/></td>
        </tr>

        <tr>
            <th>입고날짜</th>
            <td><input type="date" id="startday" name="startday" value="<%=read.startday%>"/></td>
            <th>출고날짜</th>
            <td><input type="date" id="endday" name="endday" value="<%=read.endday%>"/></td>
        </tr>

        <tr>
            <th>제품 시리얼 번호</th>
            <td><input type="text" name="codeserial" value="<%=read.codeserial%>" style="width:50%;height:100%;font-size:15px;" required/></td>
            <th>고객 접수 증상</th>
            <td><input type="text" name="clientsym" id="clientsym" style="width:50%;height:50%;font-size:15px;" value="<%=read.clientsym%>" required/></td>

        </tr>

        <tr>
            <th>고장 증상</th>
            <td><input type="text" name="repairsym" id="repairsym" style="width:50%;height:50%;font-size:15px;" value="<%=read.repairsym%>" required/></td>
            </td>
            <th>이미지 등록</th>
            <td><input type="file" name="photo"/></td>
        </tr>

        <tr>
            <th>수리내역</th>
            <td colspan="4"><input type="text" style="width: 99%;" value="<%=read.comment%>" name="comment" id="comment" required/></td>
        </tr>

        <tr>
            <td colspan="6"><input type="submit" id="submit" value="수정하기" style="text-align:center; display:block; margin: 0 auto;" ></td>
        </tr>
        </tbody>

    </table>
</form>

</body>
</html>
